<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="PPQ.Navigation.LoginShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:PPQ.Views.LoginShell"
    Title="PPQ">

    <FlyoutItem>
        <ShellContent
            Title="{DynamicResource ButtonLabelLogin}"
            ContentTemplate="{DataTemplate local:LoginPage}"
            Route="LoginPage" />

        <ShellContent
            Title="{DynamicResource TitleLabelRegister}"
            ContentTemplate="{DataTemplate local:RegisterPage}"
            Route="RegisterPage" />

        <ShellContent
            Title="{DynamicResource TitleLabelResetPass}"
            ContentTemplate="{DataTemplate local:ResetPasswordPage}"
            Route="ResetPasswordPage" />
    </FlyoutItem>
</Shell>
